import * as fs from 'fs';
import _ from 'underscore';
import timeSpan from 'time-span';
import cloneDeep from 'clone-deep';

class Model {
  constructor() {}

  endBlock = () => {
    /**
     * EndBlock can cause a complete change in the validator set
     * and the powers of the validators
     *
     */
  };
}

function main() {
  const m = new Model();
}

console.log(`Running main`);
main();
